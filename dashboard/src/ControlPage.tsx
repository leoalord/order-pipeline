export function ControlPage() {
  return (
    <main className="page">
      <h1>Control</h1>
      <p className="lede">
        Load and fault controls land in a later slice. Watch is the live view;
        this route is reserved so the proxy does not have to be retrofitted.
      </p>
    </main>
  );
}
